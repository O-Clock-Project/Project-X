{% extends 'base.html.twig' %}

{% block title %}Mail d'inscription{% endblock %}

{% block stylesheets %}
    <link href="{{ asset('css/reset.css') }}" rel="stylesheet">
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}

    <div id="register">
        <img id= "register-title-logo" src=" {{ asset('images/logo_bold_jaune.png') }} ">
        <h2 id="register-subtitle">Pré'Inscription des éléves</h2>
        
        {% for message in app.flashes('notice') %}
            <div > {{ message }}</div>
        {% endfor %}
        
        <form "{{ path("sendEmailRegistration")}}" method="POST" id="register-form" action="" autocomplete="off">
            
            <div class="register-form-category">
                <label class="register-form-label" for="email">Email</label>
                <textarea class="register-form-input" id="email" name="email">
                </textarea>
            </div>

            <div class="register-form-category">
                
                <label class="register-form-label" for="promotion">Promotion</label>
                <select class="register-form-input" type="select" id="Promotion" name="Promotion">
                {% for promotion in promotions %} 
                    <option value={{ promotion.id|e}}>{{ promotion.name|e}}</option> 
                {% endfor %}
                </select> 
            </div>
            <button type="submit" class="register-form-btn">Envoyer</button>
        </form> 
        
    
    </div>

{% endblock %}